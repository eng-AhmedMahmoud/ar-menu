# Image-to-3D provider. "meshy" (default, returns glb + usdz) or "tripo".
MODEL_PROVIDER=meshy
MESHY_API_KEY=
# TRIPO_API_KEY=

# Set after the first deploy, then run `pnpm qr`. QR codes encode this.
SITE_URL=
